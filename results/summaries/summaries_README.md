# results/summaries/

Reserved for future higher-level summary write-ups (e.g. a one-page results
digest separate from the full report). Currently, summary statistics from
the Monte Carlo analysis are saved as a table in `results/tables/` instead
(`uncertainty_summary.csv`), which covers this project's present needs.
